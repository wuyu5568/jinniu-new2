# ADR 0013: 提取利率掉头只翻方向，下次结算仍按当前档计息

**Status**: superseded by ADR 0017

提取确认时对进行中认购单只翻转静态收益率方向（数字不变），并标记本结算周期已掉头；同周期内再提取不重复翻转。下次静态收益结算仍按当前档计息，发完后再按新方向照常步进并清标记。避免「提取时立刻降档」或「下次结算先步进再计息」打乱逐日档位（例如 0.65% 提现后应再按 0.65% 结算一次，其后才是 0.60%）。

已被 ADR 0017 废止：现改为符合条件时提现立刻步进，且 down / 最低档 up 不掉头。
